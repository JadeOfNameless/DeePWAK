\begin{algorithm}[H]
  \caption{DeePWAK constructor}
  \DontPrintSemicolon  % Don't print semicolons

  \Dat{\DeePWAK{$\mathcal{E},\mathcal{K},\mathcal{D}$}}{

    {$\forall m,d,k$ : $\mathbb{N}$ \tcp*{input dimension, embedding dimension, max clusters}}
    \KwIn{}
    $\mathcal{E}$ : $\Arch{m,d}$ \tcp*{encoder architecture}
    $\mathcal{K}$ : $\Arch{m,k}$ \tcp*{partitioner architecture}
    $\mathcal{D}$ : $\Arch{d,m}$ \tcp*{decoder architecture}
    
    \KwOut{}
    $\mathcal{W} : \Params(\mathcal{E}) \to \Params(\mathcal{K}) \to \Params(\mathcal{D}) \to \Model(m,m)$
    }
\end{algorithm}

